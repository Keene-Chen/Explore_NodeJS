const axios = require('axios');
var url = 'http://134.175.221.21:8001/login';

axios
  .post(url, {
    password: '123456',
    username: 'admin',
  })
  .then((response) => {
    console.log(response.data.data.userInfo.name);
  })
  .catch((error) => {
    console.log(error);
  });
